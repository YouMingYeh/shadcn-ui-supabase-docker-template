# ====================================================================================
# Mars Monorepo - Docker Compose Environment Variables
# ====================================================================================
# This file serves as a template for the root .env file
# The root .env is used ONLY for docker-compose.yml variable substitution
# Copy this file to .env and fill in your actual values
#
# IMPORTANT:
# - Root .env is for Docker Compose only (gitignored)
# - Each app also needs its own .env file - see apps/*/. env.example
# - For development, use .env.local files in each app directory
# ====================================================================================

# ======================
# Server Configuration
# ======================
# CORS_ORIGIN: Use "*" to allow all origins, or comma-separated list for specific origins
# Examples:
#   CORS_ORIGIN=*  (allow all - recommended for development)
#   CORS_ORIGIN=https://yourdomain.com,https://admin.yourdomain.com  (production)
CORS_ORIGIN=*

# ======================
# Supabase Configuration
# ======================
# Get these from: https://supabase.com/dashboard → Your Project → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_ANON_KEY=your-anon-key-here

# ======================
# Next.js Public Variables
# ======================
# These are injected into Next.js builds via build args
# They will be baked into the client-side JavaScript bundle
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
NEXT_PUBLIC_API_URL=http://localhost:4000

# NEXT_PUBLIC_SITE_URL: The public-facing URL of your web app (used for OAuth redirects)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ======================
# Admin Panel Configuration
# ======================
# Admin password hash (bcrypt)
# Generate a new hash: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
# IMPORTANT: Dollar signs ($) must be double-escaped as $$ in .env files
# Example: ADMIN_PASSWORD_HASH=$$2b$$10$$abc123...
ADMIN_PASSWORD_HASH=$$2b$$10$$vGY4gBTPkfwJxUw7Y6VyUeN3UEdK9PowhCefwyA4LDii53kY6rZ1W

# ======================
# Python Service Configuration
# ======================
# OpenAI API key for embeddings generation
OPENAI_API_KEY=your-openai-api-key-here
