# ====================================================================================
# Admin App - Local Development Environment Variables
# ====================================================================================
# Copy this file to .env.local and fill in your actual values
# .env.local is gitignored and used for local development only
#
# For Docker/Production: Values are injected via docker-compose.yml
# ====================================================================================

# ======================
# Supabase Configuration
# ======================
# Get these from: https://supabase.com/dashboard → Your Project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
# Service role key for admin operations (keep secret!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ======================
# Admin Authentication
# ======================
# Generate password hash using: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
ADMIN_PASSWORD_HASH=$2a$10$example.hash.replace.with.your.own

# Session secret (min 32 characters) - generate with: openssl rand -base64 32
SESSION_SECRET=your-secret-key-min-32-characters-long

# ======================
# API Configuration (Optional)
# ======================
# Uncomment if you need to customize the API URL for the browser
# NEXT_PUBLIC_API_URL=http://localhost:4000
