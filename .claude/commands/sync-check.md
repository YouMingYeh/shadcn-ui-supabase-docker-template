---
argument-hint: [model or endpoint]
description: Check iOS/backend model sync
---

Use the ios-backend-sync-checker agent to verify: $ARGUMENTS

If no arguments, ask which DTO/model or endpoint to check.
